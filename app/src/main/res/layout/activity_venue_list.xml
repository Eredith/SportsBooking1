<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white">

    <!-- Header Section -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_header"
        android:padding="16dp">

        <TextView
            android:id="@+id/headerTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Temukan Beragam Venue!"
            android:textSize="18sp"
            android:textColor="@android:color/white"
            android:textStyle="bold" />

        <ImageView
            android:id="@+id/iconFilter"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:src="@drawable/ic_filter" />
    </RelativeLayout>

    <!-- Search Bar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/header"
        android:orientation="horizontal"
        android:background="@android:color/white"
        android:padding="12dp"
        android:elevation="4dp"
        tools:ignore="NotSibling">

        <EditText
            android:id="@+id/searchVenue"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:hint="Cari Venue"
            android:minHeight="48dp"
            android:padding="10dp" />

        <ImageView
            android:id="@+id/searchIcon"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_search" />
    </LinearLayout>

    <!-- Filters -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/searchVenue"
        android:orientation="horizontal"
        android:padding="10dp"
        android:gravity="center"
        tools:ignore="NotSibling">

        <!-- Filter buttons, like Bulu Tangkis, Sepak Bola, etc. -->
        <Button
            android:id="@+id/filterBadminton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Bulu Tangkis"
            android:backgroundTint="@android:color/holo_orange_dark" />

        <Button
            android:id="@+id/filterSepakBola"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Sepak Bola"
            android:layout_marginStart="8dp"
            android:backgroundTint="@android:color/darker_gray" />
    </LinearLayout>

    <!-- RecyclerView for Venue List -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/venueRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/filterBadminton"
        android:layout_marginTop="16dp"
        android:padding="8dp"
        android:clipToPadding="false"
        tools:ignore="NotSibling" />

</RelativeLayout>
